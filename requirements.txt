# ArkaFon backend — build-time only.
# No web server, no DB, no auth: every prior runtime dep was dropped along
# with src/api.py during the Jamstack migration.

# Core data pipeline.
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0          # parquet read/write

# Source feeds.
tefas-client             # TEFAS daily scraper (used by updater.py)
yfinance>=0.2.0          # USD/TRY + gold spot (used by market_data.py)
requests>=2.28.0         # transitive but pinned because tefas-client is unpinned

# Tests.
pytest>=7.4.0
